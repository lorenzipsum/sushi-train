spring:
  flyway:
    enabled: true
    locations: classpath:db/migration

logging:
  level:
    org.springframework.orm.jpa.JpaTransactionManager: DEBUG

app:
  belt:
    placement:
      max-new-plates-per-request: 10